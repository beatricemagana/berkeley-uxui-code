// Create the addNumbers function here.

function addNumbers() {
    const two = 2;
    const three = 3;
    const five = two + three;
    console.log(five);
}

addNumbers();

// Create the stringConcat function here.

function stringConcat() {
    const taco = 'taco';
    const cat = 'cat';
    const tacoCat = taco + cat + ' spelled backwards is ' + taco + cat;

    alert(tacoCat);
}

stringConcat();
